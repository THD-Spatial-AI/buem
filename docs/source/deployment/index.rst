Deployment
==========

BuEM is designed to run as a Docker container. See the
:doc:`../api_integration/docker_setup` page for building the image,
``docker compose up``, and environment variables.

.. toctree::
   :maxdepth: 2

   production_deployment

For most users, the default ``docker-compose.yml`` in the repository is
sufficient. The page below covers additional production hardening tips.